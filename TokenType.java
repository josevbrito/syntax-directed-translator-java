/**
 * TokenType.java
 * Defines the TokenType enum and the Token class used by the Scanner.
 */

public enum TokenType {
    // Operators
    PLUS, MINUS,
    // Literals
    NUMBER,
    // End of file
    EOF
}
